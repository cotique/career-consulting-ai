import { describe, expect, it } from 'vitest';

// Placeholder to prove the Vitest runner is wired up correctly (T4).
// Delete once real unit tests exist alongside actual modules.
describe('sanity', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
